This directory is for holding site-local roles and other code that
should *not* be shared publicly and committed to GitHub.  The
top-level ``.gitignore`` file makes it such that it is by default
ignored by Git.
