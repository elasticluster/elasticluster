---
mesos_install_mode: "master" # {master|slave}

quorum_size: "{{groups['mesos_master']|length}}"
mesos_quorum: "{% if quorum_size == '1' %}{{quorum_size}}{% else %}{{((quorum_size|int/3)*2)|round|int}}{% endif %}"
