# jupyter/meta/main.yml
---

dependencies:
  # install Anaconda Python3 to run the Jupyter Notebook itself
  - role: anaconda
    anaconda_python_version: '3'
    anaconda_in_path: true
  # ...but also install Anaconda Python2 because Py2 is still more widely used
  - role: anaconda
    anaconda_python_version: '2'
    anaconda_in_path: false
  - role: r
