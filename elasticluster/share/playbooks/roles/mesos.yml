---

- name: Install Mesos master node
  hosts: mesos_master
  vars:
    java_version: 8
  tags:
    - zookeeper
    - mesos
    - marathon
    - master
  roles:
    - zookeeper
    - mesos-master
    - marathon


- name: Install Mesos slave nodes
  hosts: mesos_slave
  vars:
    java_version: 8
  tags:
    - docker
    - mesos
    - slave
  roles:
    - docker
    - mesos-slave
