---

- name: Install Mesos master node
  hosts: mesos_master
  tags:
    - zookeeper
    - mesos
    - spark
    - master
  roles:
    - mesos-master


- name: Install Mesos slave nodes
  hosts: mesos_slave
  tags:
    - docker
    - mesos
    - spark
    - slave
  roles:
    - docker
    - mesos-slave
