# spark-common/meta/main.yml
---

dependencies:
  # mirror jupyter settings regarding anaconda, otherwise master will differ from worker nodes
  - role: anaconda
    anaconda_python_version: '3'
    anaconda_in_path: true
  - role: anaconda
    anaconda_python_version: '2'
    anaconda_in_path: false
  - { role: bigtop }
  - { role: r }
